// src/lib/demo.ts
// ─────────────────────────────────────────────────────────────
// The locked demo path (ROADMAP 1.8): scenario E hero claim in
// the hero district. Everything demo-critical references these
// constants instead of deriving them from engine output, so the
// drill-down target can never drift when data/engine change.
// ─────────────────────────────────────────────────────────────

export const HERO_CLAIM_ID = "MP-DIN-HERO-001";
export const HERO_DISTRICT = "Dindori";
export const HERO_STATE = "Madhya Pradesh";
